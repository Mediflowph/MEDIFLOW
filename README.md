
  # MediFlow Drug Inventory Dashboard

  This is a code bundle for MediFlow Drug Inventory Dashboard. The original project is available at https://www.figma.com/design/1MXl5sZ3nHkveCY0S1JXHj/MediFlow-Drug-Inventory-Dashboard.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  